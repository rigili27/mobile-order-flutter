import 'package:flutter/material.dart';

/// Paleta de marca LABGE (violeta + negro) — misma que el tema 'labge' del
/// ERP (ver GestionERP/config/themes.php: --series-1 / --nav-active-bg y
/// --nav-bg respectivamente), para que esta app y el panel admin se vean
/// como una sola marca.
const kLabgeVioleta = Color(0xFF7C5CFC);
const kLabgeNegro = Color(0xFF121214);
